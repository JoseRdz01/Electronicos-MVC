@model IEnumerable<Electronicos_MVC.Models.Marcas>

@{
    ViewBag.Title = "Index";
}

<h2 class="mb-4">Marcas</h2>

<p>
    @Html.ActionLink("Crear Nueva Marca", "Create", null, new { @class = "btn btn-primary mb-3" })
</p>

<table class="table table-striped table-hover table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nombre_Marca)</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ID_Marca)</td>
                <td>@Html.DisplayFor(modelItem => item.Nombre_Marca)</td>

                <td>
                    <a href="@Url.Action("Edit", new { id = item.ID_Marca })" class="btn btn-sm btn-warning">Editar</a>
                    <a href="@Url.Action("Details", new { id = item.ID_Marca })" class="btn btn-sm btn-info">Detalles</a>
                    <a href="@Url.Action("Delete", new { id = item.ID_Marca })" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
