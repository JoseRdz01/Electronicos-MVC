@model IEnumerable<Electronicos_MVC.Models.bitacora_categorias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Accion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Accion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Bitacora }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Bitacora }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Bitacora })
        </td>
    </tr>
}

</table>
